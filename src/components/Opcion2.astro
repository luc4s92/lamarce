<section class="hero">
  <h1>AnunciAds (opcion 2)</h1>
  <p>Escalá tus anuncios</p>
  <div class="btn">CTA</div>
</section>

<section class="services">
  <h2>Servicios</h2>
  <p>Ads · Testing · Optimización</p>
</section>

<section class="contact">
  <h2>Contacto</h2>
  <div class="btn">Enviar</div>
</section>

<style>
  :root {
    --orange: #ff7a00;
    --dark: #0f172a;
    --gray: #cbd5e1;
  }

  section {
    margin: 0;
    font-family: Inter, system-ui;
    background: white;
    color: var(--dark);
    min-height: 100vh;
    padding: 4rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 1rem;
  }

  .hero {
    background: var(--dark);
    color: white;
  }

  .services {
    background: white;
  }

  .contact {
    background: #f8fafc;
  }

  .btn {
    background: var(--orange);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    width: fit-content;
  }
</style>
