<section class="hero">
  <h1>AnunciAds (opcion 3)</h1>
  <p>Anuncios que convierten</p>
  <div class="btn">CTA</div>
</section>

<section class="services">
  <h2>Servicios</h2>
  <p>Google Ads · Meta Ads · Optimización</p>
</section>

<section class="contact">
  <h2>Contacto</h2>
  <div class="btn">Enviar</div>
</section>

<style>
  :root {
    --orange: #ff6a00;
    --dark: #111827;
    --bg: #f9fafb;
  }

  section {
    margin: 0;
    font-family: Inter, system-ui;
    background: var(--bg);
    color: var(--dark);
    min-height: 100vh;
    padding: 4rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 1rem;
  }

  .hero {
    background: white;
  }

  .services {
    background: var(--bg);
  }

  .contact {
    background: var(--dark);
    color: white;
  }

  .btn {
    background: var(--orange);
    color: white;
    padding: 0.75rem 1.5rem;
    width: fit-content;
    border-radius: 8px;
    font-weight: 600;
  }
</style>
